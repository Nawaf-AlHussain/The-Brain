# Lightweight dependency set — used by Vercel/serverless builds.
# (MinerU/RAG-Anything and the local reranker model exceed serverless size limits.)
#
# For the full feature set (PDF/DOCX parsing via MinerU, local reranker),
# use requirements-full.txt — see Dockerfile.thebrain.

# FastAPI server
fastapi
uvicorn
python-multipart

# HTTP client (LLM providers, external reranker)
httpx

# Numeric arrays (llm_providers.py)
numpy

# RAG core (LightRAG)
# NOTE: openai and tiktoken are required at import time (lightrag.llm.openai,
# tokenization). lightrag-hku declares them optional/pipmaster-managed, but in
# read-only serverless environments they MUST be installed explicitly.
lightrag-hku==1.5.7
openai>=1.50.0
tiktoken>=0.7.0

# Knowledge graph DB
neo4j
